✋ Hand Frame FX — Windows XP Edition
Real-time hand tracking with MediaPipe, running inside a Windows XP desktop — pure HTML/CSS/JS, no frameworks.

What it does
Raise both hands to create a glowing frame between your fingertips. Everything inside the frame gets a live camera effect; outside goes dark and blurred. Pinch thumb+index to switch effects.
